-- Config: Stores game settings that can be easily configured.

local Config = {
    MinPlayers = 2,
    IntermissionDuration = 10, -- seconds
    Map = {
        SpawnPosition = Vector3.new(0, 50, 0)
    }
}

return Config
